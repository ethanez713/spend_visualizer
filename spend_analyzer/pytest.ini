[pytest]
markers =
    e2e: slow real-browser tests (excluded by default; run: pytest tests/e2e -m e2e)
# Fast suites (unit + headless AppTest UI) run on every `pytest`; the browser
# suite is opt-in so it isn't paid on every change.
addopts = -m "not e2e"
