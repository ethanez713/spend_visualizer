# Secrets — never commit (broad defensive patterns)
.env*
!.env.example
*secret*.json
token.json

# Plaid local state (access tokens, cursors) — keep private
tokens.json
sync_cursors.json

# Atomic-write temp files
*.tmp

# Output
transactions.csv
transactions_raw.jsonl.xz

# Logs
*.log
logs/

# Python
venv/
__pycache__/
*.pyc
