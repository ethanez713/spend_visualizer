"""plaid_category_transformer — re-categorize low-confidence Plaid transactions.

Real code lives here in ``src/``; the root ``categorize.py`` shim is the entry point.
See ``transformer.py`` for the pipeline and ``PLAN.md`` for the full spec.
"""
