# Durable-persist extras: Google Drive client libs for the persister library.
# Only needed for the --persist path (reconcile + Drive sync); the core sync +
# local CSV pipeline needs only requirements.txt.
#
# Install from the hash-locked tree (see requirements.lock.txt), then add the
# sibling persister library editable — editables can't be hash-pinned, so it is
# a separate, hash-free step (its own deps are already locked here):
#   ./venv/bin/pip install --require-hashes -r requirements.lock.txt
#   ./venv/bin/pip install --no-deps -e ../persister
# (relative path — run pip from this component's root; rebuild the venv if either
# component is moved/renamed)

# Google Drive (lazy-imported inside persister.drive_sync).
google-api-python-client==2.197.0
google-auth==2.53.0
google-auth-oauthlib==1.4.0
