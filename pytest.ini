[pytest]
# Put the package dir (src/) on sys.path so tests can `import persister`
# even before `pip install -e .` is run.
pythonpath = src
testpaths = tests
# Tests are named given_<precondition>_when_<action>_then_<result> (house style).
python_functions = given_* test_*
