[pytest]
# Put the project root on sys.path so tests can `import plaid_client` etc.,
# while keeping the test files themselves in tests/.
pythonpath = .
testpaths = tests
# Tests are named given_<precondition>_when_<action>_then_<result>.
python_functions = given_* test_*
