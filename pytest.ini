[pytest]
# Put the repo root on sys.path so tests can `import src.pipeline` (matching the
# run.py shim and the sibling repos' layout).
pythonpath = .
testpaths = tests
# House test-naming style: given_<precondition>_when_<action>_then_<result>.
python_functions = given_* test_*
