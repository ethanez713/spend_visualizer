You're building a new project from scratch in this directory (~/plaid_category_transformer). Read ./PLAN.md in full before doing anything — it's the complete spec. Implement Project C — plaid_category_transformer (the "PROJECT C" section), plus the "Shared context" conventions and the global security baseline it references.
▎
▎ This tool re-categorizes LOW/MEDIUM-confidence Plaid transactions within Plaid's official PFC taxonomy: select those rows → mechanical rules (use ALL signals: merchant_entity_id, counterparties, website, name/original_description, payment_channel, amount) → a local LLM (Ollama qwen2.5:7b) that sees a schema description + the vendored Plaid PFC taxonomy → overwrite the wrong category in place, preserving originals in new nullable columns + a provenance flag → persist via the persister library.
▎
▎ Read these siblings for patterns — mirror closely:
▎ - ~/converter/src/reviewer.py — the local-LLM pattern to adapt: instructor.from_openai(OpenAI(base_url=host+"/v1", api_key="ollama"), mode=JSON), qwen2.5:7b, temperature=0 seed=0, batches of ~35, _ensure_ready/_ping/_ensure_model, _spinner, graceful skip if Ollama is down, pydantic response models, injected-taxonomy system prompt.
▎ - ~/converter/src/{converter.py,config.py,categories.py} — mechanical cascade + merchant memory + normalize_merchant + contains_word + offline/test conventions.
▎ - ~/transactions/src/fetch_transactions.py — the 54-col schema, txn_to_row, _csv_safe; the raw record shape you operate on.
▎
▎ Dependency: this project imports persister (Project A). If ~/persister isn't built yet, build against the persister public API exactly as specified in PLAN.md and stub it in tests; pip install -e ../persister once it exists. Vendor Plaid's PFC taxonomy as a committed local module (don't fetch at runtime). Per-project .venv, pinned deps, offline unit tests (stub the LLM), and Ollama-gated integration tests with threshold (not exact-match) assertions.
▎
▎ When built and tested: git init, and after confirming with me create a PRIVATE GitHub repo <your-github-user>/plaid_category_transformer — it commits real financial data under data/, so it must be private. Ask me before any live Drive run.
